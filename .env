# Backend base URL — includes the /coduel context-path.
# Dev points at the local Spring Boot app. In prod, override this with the deployed
# backend origin (via your host's build env vars), e.g. https://api.coduel.app/coduel
VITE_API_BASE_URL=http://localhost:8080/coduel
